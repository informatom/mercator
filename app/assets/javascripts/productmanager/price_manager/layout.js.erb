$(function () {
  w2utils.locale('/assets/contentmanager/de-de.json')

  $('#grid').w2layout({
    name: 'pricemanager',
    panels: [
      {
        type: 'top',
        title: '<%= I18n.t("mercator.price_manager.title") %>',
        size: 58,
        resizable: true,
        content: "<div id='menu'></div>"
      }, {
        type: 'left',
        size: '25%',
        resizable: true,
        title: '<%= I18n.t("activerecord.models.product.one") %>',
        content: "<div id='productForm' style='height: 100%'></div>"
      }, {
        type: 'main',
        resizable: true,
        title: '<%= I18n.t("activerecord.models.inventory.other") %>',
        content: "<div id='inventoriesGrid' style='height: 100%'></div>"
      }, {
        type: 'preview',
        resizable: true,
        size: '450',
        title: '<%= I18n.t("activerecord.models.inventory.one") %>',
        content: "<div id='inventoryForm' style='height: 421px'></div>"
      }, {
        type: 'right',
        size: '35%',
        resizable: true,
        content: "pricelayout" }
    ],
    onRender: function(event) {
      event.onComplete = function () {
        productForm()
        inventoriesGrid()
        inventoryForm()
      }
    }
  })

  $().w2layout({
    name: 'pricelayout',
    panels: [
      {
        type: 'main',
        size: '50%',
        resizable: true,
        title: '<%= I18n.t("activerecord.models.price.other") %>',
        content: "<div id='propertyGroupsGrid' style='height:100%;'></div>"
      },
      {
        type: 'preview',
        resizable: true,
        title: '<%= I18n.t("activerecord.models.price.other") %>',
        content: "<div id='propertiesGrid' style='height:100%;'></div>"
      }
    ],
    onRender: function(event) {
      event.onComplete = function () {
//        propertyGroupsGrid()
//        w2ui.propertyGroupsGrid.load('/productmanager/property_manager/show_property_groups')
//        propertiesGrid()
//        w2ui.propertiesGrid.load('/productmanager/property_manager/show_properties')
      }
    }
  })

  w2ui.pricemanager.content('right', w2ui['pricelayout'])
})
