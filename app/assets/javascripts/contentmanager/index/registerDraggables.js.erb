function registerDraggables() {
  $('.draggable').draggable({
    cursor: 'crosshair',
    opacity: 0.9,
    helper: "clone",
    containment: ".myLayout",
    zIndex: 10000,
    appendTo: "body",
    connectToFancytree: true,
    cursorAt: { top: 10, left: -10 }
  })
}